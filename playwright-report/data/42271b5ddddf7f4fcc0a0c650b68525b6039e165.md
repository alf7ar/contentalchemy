# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: responsiveness.spec.ts >> Mobile Responsiveness >> Dashboard fits mobile viewport
- Location: tests\responsiveness.spec.ts:21:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('textarea')
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for locator('textarea')

```

```yaml
- img
- heading "This page couldn’t load" [level=1]
- paragraph: Reload to try again, or go back.
- button "Reload"
- button "Back"
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test"
  2  | 
  3  | test.describe("Mobile Responsiveness", () => {
  4  |   test.use({ viewport: { width: 375, height: 812 } })
  5  | 
  6  |   test("Landing page is usable on mobile", async ({ page }) => {
  7  |     await page.goto("/ar")
  8  |     await expect(page.locator("h1")).toBeVisible()
  9  |     await page.locator('[class*="md:hidden"]').first().click()
  10 |     await expect(page.getByRole("link", { name: "الباقات" })).toBeVisible()
  11 |   })
  12 | 
  13 |   test("Pricing page stacks properly on mobile", async ({ page }) => {
  14 |     await page.goto("/ar/pricing")
  15 |     const planCards = page.locator(".rounded-3xl").filter({ has: page.locator("h3") })
  16 |     const count = await planCards.count()
  17 |     expect(count).toBe(4)
  18 |     await expect(planCards.first()).toBeVisible()
  19 |   })
  20 | 
  21 |   test("Dashboard fits mobile viewport", async ({ page }) => {
  22 |     await page.goto("/ar/dashboard")
> 23 |     await expect(page.locator("textarea")).toBeVisible()
     |                                            ^ Error: expect(locator).toBeVisible() failed
  24 |     await page.evaluate(() => {
  25 |       const body = document.body
  26 |       const html = document.documentElement
  27 |       const height = Math.max(body.scrollHeight, html.scrollHeight)
  28 |       const width = Math.max(body.scrollWidth, html.scrollWidth)
  29 |       return { width, height }
  30 |     })
  31 |   })
  32 | })
  33 | 
  34 | test.describe("Tablet Responsiveness", () => {
  35 |   test.use({ viewport: { width: 768, height: 1024 } })
  36 | 
  37 |   test("All pages render correctly on tablet", async ({ page }) => {
  38 |     await page.goto("/ar")
  39 |     await expect(page.locator("h1")).toBeVisible()
  40 |     await page.goto("/ar/pricing")
  41 |     await expect(page.locator("h1")).toBeVisible()
  42 |     await page.goto("/ar/dashboard")
  43 |     await expect(page.locator("h1")).toBeVisible()
  44 |   })
  45 | })
  46 | 
```